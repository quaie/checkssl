# ... prior content ...
inputs:
  domains-file:
    description: 'Domains file path'
    default: 'domains.txt'
# In run step:
run: |
  python ${{ github.action_path }}/testssl.py --domains "${{ inputs.domains-file }}" --json > sslyze-results.json
# ...
